# UniConvert 🔄

A lightweight, secure, and completely client-side file converter and merger tool. No server uploads required!

## 🚀 Features

- **Merge PDFs**: Combine multiple PDF files into a single document.
- **Word to PDF**: Convert `.docx` files to PDF (text-heavy documents).
- **Images to PDF**: Convert JPG and PNG images into a PDF document.
- **100% Client-Side**: All processing happens in your browser. Your files never leave your device.
- **Single File App**: The entire application is contained in one `index.html` file.

## 🛠️ Tech Stack

- **React**: UI built with React (loaded via CDN).
- **Tailwind CSS**: Styling (loaded via CDN).
- **pdf-lib**: For PDF manipulation and merging.
- **mammoth.js**: For extracting text from Word documents.
- **jsPDF**: For generating PDFs from text.

## 📖 How to Use

1.  **Download**: Clone this repository or simply download the `index.html` file.
2.  **Run**: Double-click `index.html` to open it in your web browser.
3.  **Convert**: Drag and drop your files to start converting!

## 📦 Installation

Since this is a standalone HTML file, no installation is required (no `npm install`, no `node_modules`).

```bash
git clone https://github.com/ompop-pro21/uni-convert.git
cd uni-convert
# Open index.html in your browser
```

## 📝 License

This project is open source and available under the [MIT License](LICENSE).
